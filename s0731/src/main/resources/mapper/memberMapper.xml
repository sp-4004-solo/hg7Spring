<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.mapper.MMapper">
	<select id="idck" resultType="com.java.dto.Member">
		select * from member where id=#{id}
	</select>
</mapper>
